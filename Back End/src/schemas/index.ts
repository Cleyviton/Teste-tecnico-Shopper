import {
    userSchema,
    userSchemaRequest,
    userSchemaResponse,
    loginSchema,
} from "./user.schemas";

const schemas = {
    userSchema,
    userSchemaRequest,
    userSchemaResponse,
    loginSchema,
};

export default schemas;
