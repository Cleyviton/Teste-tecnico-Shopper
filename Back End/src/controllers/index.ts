import {
    validateCsv,
    updateProducts,
    listProducts,
} from "./products.controllers";

const controllers = {
    validateCsv,
    updateProducts,
    listProducts,
};

export default controllers;
