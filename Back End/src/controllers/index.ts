import {
    validateCsv,
    updateProducts,
    listProducts,
    listPacks,
} from "./products.controllers";

const controllers = {
    validateCsv,
    updateProducts,
    listProducts,
    listPacks,
};

export default controllers;
